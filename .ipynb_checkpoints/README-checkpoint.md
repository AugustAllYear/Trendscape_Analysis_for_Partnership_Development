# Trendscape_Analysis_for_Partnership_Development

Tech Req notes
`nltk` is used for stop words. After installation the stop words corpus must be downloaed.

`import nltk`
nltk.download('stopwords')

Builtin Python Modules:
`re` , `collections`, `ast`, `warnings` 
